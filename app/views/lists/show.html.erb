<ul class="nav justify-content-end">
  <li class="nav-item">
    <a class="nav-link active" aria-current="page" href="#">My List</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="#">My Account</a>
  </li>
</ul>
<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= asset_path('banner.jpg') %>)">
  <div class="container" >
    <h1><%= @list.name %></h1>
    <p>"Cinema is the most beautiful fraud in the world."</p>
    <%= link_to "Go back to all lists", lists_path, class:"btn btn-light"%>
    <br>
    <br>
    <%= link_to "Delete this list", list_path(@list), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class:"btn btn-ghost"%>
  </div>
</div>
<br>

<div class="container text-center">
    <div class="row">
    <div class="col">
  <% @list.movies.each do |movie| %>
<div class="card" style="width: 18rem;">
  <img src="..." class="card-img-top" alt="...">
  <div class="card-body">
    <h5 class="card-title"><%= movie.title %></h5>
    <p class="card-text"><%= movie.overview %></p>
    <hr>
    <% bookmark = Bookmark.find_by(list: @list, movie: movie) %>
        "<%= bookmark.comment %>"
  </div>
</div>
  <% end %>
  </div>
</div>
</div>
